import React from "react";

import "./App.scss";
import Auth from "./Auth";

const App = (): JSX.Element => {
  return (
    <React.Fragment>
      <Auth />
    </React.Fragment>
  );
};

export default App;
