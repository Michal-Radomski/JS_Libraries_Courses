import React from "react";

import "./App.scss";
import RTK_QueryComponent from "./RTK_QueryComponent";

const App = (): React.JSX.Element => {
  return (
    <React.Fragment>
      <RTK_QueryComponent />
    </React.Fragment>
  );
};

export default App;
